name: Keep Render Awake

on:
  push:
    branches: ["main"]   # TEMPORARY
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:


jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: |
          curl -L https://predimaint-machinery-prediction.onrender.com/
